# Copy file này thành .env ở môi trường development/production
# CRA yêu cầu biến phải có tiền tố REACT_APP_
# Giá trị nên bao gồm full host + prefix /api/v1 để code chỉ gọi đường dẫn tương đối như /auth/login
# Ví dụ môi trường cục bộ:
# REACT_APP_API_BASE_URL=http://localhost:5000/api/v1
# Ví dụ môi trường server:
# REACT_APP_API_BASE_URL=http://160.30.21.113:5000/api/v1
REACT_APP_API_BASE_URL=/api/v1 